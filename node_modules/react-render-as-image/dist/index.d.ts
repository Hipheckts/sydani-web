import * as React from 'react';
import './styles.scss';
export declare type IRenderAsImageProps = {
    children: React.ReactNode;
    width?: number | string;
    height?: number | string;
    alt?: string;
    format?: string;
};
declare const initialState: {
    imageDataUrl: string;
};
declare type State = Readonly<typeof initialState>;
declare class RenderAsImage extends React.Component<IRenderAsImageProps> {
    private myDiv;
    readonly state: State;
    componentDidMount: () => void;
    render: () => JSX.Element;
}
export default RenderAsImage;
